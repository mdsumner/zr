[package]
name = "zr"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
name = "zr"

[dependencies]
extendr-api = "0.8"
zarrs = { version = "0.23", features = ["ndarray", "filesystem", "gzip", "blosc", "zstd", "sharding"] }
serde_json = "1"
