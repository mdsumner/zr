% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zr.R
\name{zr_read}
\alias{zr_read}
\title{Read array data}
\usage{
zr_read(arr, offset = NULL, count = NULL)
}
\arguments{
\item{arr}{A ZrArray.}

\item{offset}{Numeric vector of 0-based start indices, or NULL for full read.}

\item{count}{Numeric vector of counts per dimension, or NULL for full read.}
}
\value{
Numeric or integer vector, matrix, or array.
}
\description{
Read the entire array or a subset.
}
\details{
For 2D arrays, returns a matrix oriented for \code{\link[=ximage]{ximage()}} / \code{\link[=rasterImage]{rasterImage()}}:
\code{matrix(data, nrow, ncol, byrow = TRUE)}. This means the first
dimension varies along rows and the second along columns, matching
the C-contiguous (row-major) order in which zarr stores data.

For 1D arrays, returns a plain vector. For 3D+ arrays, returns a
vector with a \code{"zr_shape"} attribute — use \code{\link[=zr_as_matrix]{zr_as_matrix()}} or
construct your own array from the flat data and shape.

\strong{Indexing is 0-based} (matching zarr/Python convention): \code{offset = 0}
means the first element.
}
